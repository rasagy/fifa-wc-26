# Project Ledger: groups-simple-scroll

| Stage | Skill | Status | Decisions / Evidence | Outputs |
| --- | --- | --- | --- | --- |
| 1. Project brief | Data Vizard | Done | Topic, focus (closest vs. most one-sided group matches), format (scrollytelling HTML), and folder convention (self-contained) set by user upfront. | `groups-simple-scroll/README.md` |
| 2. Curate data | Data Curator | Done | User approved Wikipedia as source. Fetched all 72 group-stage matches (Groups A–L) from individual Wikipedia group pages. Derived margin/total_goals/is_draw/winner/loser fields. Caveats: no penalty shootouts exist in group stage (margin=0 is a closeness floor); goal-minute data incomplete (only Group A). | `data/raw/matches_raw.csv`, `data/curated/matches_curated.csv`, `data/curated/handoff_notes.md` |
| 3. Analyze | Data Analyst | Done | User chose combined "Blowout Spectrum + Draws Aren't Equal" (two-act) direction over 3 other candidates. Key evidence: margin distribution (53% of matches decided by ≤1 goal), Germany 7-1 Curaçao & Canada 6-0 Qatar tied at margin 6, Algeria 3-3 Austria only high-scoring draw among 20. | `data/curated/analyst_handoff.md` |
| 4. Narrative | Narrator | Done | User chose Investigative frame. Story spine: opening question → full margin spread → zoom into the 20 draws → takeaway. Title: "Same Scoreboard, Different Game". | `data/curated/narrator_story_brief.md` |
| 5. Design direction | Designer | Done | User chose "Closeness beeswarm" chart form + "Stadium broadcast" aesthetic from 3 options each. | — |
| 6. Build artifact | Designer | Done | Built self-contained scrollytelling HTML with two beeswarm charts (all 72 matches by margin; 20 draws by total goals), hover/focus tooltips, scroll-reveal annotations, mobile-responsive dark stadium theme. Verified with Playwright: 72/20 circles render, 0 console errors, tooltip confirmed on hover, screenshots checked at desktop (1440px) and mobile (390px) widths across all sections. | `groups-simple-scroll/index.html` |
| 6b. Revision: group color scale | Designer | Done | Replaced draw/decisive color coding with a 12-hue categorical palette (Group A–L), one per FIFA group. Generated via OKLCH (6 base hues × 2 lightness/chroma tiers) and validated against the dataviz skill's six-checks script for the `#0b0f1a` dark surface: lightness band & chroma floor PASS; worst pairwise CVD ΔE sits at 8.3 (legal 8–12 floor band, mitigated by the added 12-item group legend + existing hover tooltip as required secondary encoding); 2 of 12 hues sit at 2.94:1 contrast (below 3:1 target, same relief mitigation applies). Re-verified with Playwright: 0 console errors, all circles render with per-group fill. | `groups-simple-scroll/index.html` |
| 6c. Revision: alphabetical hue sweep | Designer | Done | User feedback: 6b's hue assignment clustered similar hues (C/D and G/H both read green). Replaced with a single monotonic 12-hue sweep (345° span, alternating lightness/chroma per step for CVD separation) so color visibly progresses red → orange → gold → green → teal → blue → violet → magenta as groups go A → L. Searched param space and validated against dark surface: lightness band, chroma floor, and contrast all PASS; worst pairwise CVD ΔE 8.5 (floor band, same legend/tooltip mitigation). Re-verified rendering via headless Chromium screenshot (Playwright reinstall was blocked by a near-full disk; used the already-cached Chromium binary directly instead). | `groups-simple-scroll/index.html` |
| 6d. Revision: legend hover-isolate + entrance transition | Designer | Done | Added hover/focus-to-isolate on both group legends: hovering a legend swatch dims all other groups' dots to 0.12 opacity (0.2s transition) in that chart's SVG and dims the other legend items to 0.28, both charts wired independently; clears on mouseleave/blur. Added a staggered scroll-in entrance for the legend swatches (35ms stagger via IntersectionObserver + setTimeout, opacity/translateY, respects prefers-reduced-motion). Verified with Playwright: hovering Group A leaves exactly 6 (its own) of 72 circles un-dimmed, clears fully on mouse-leave, all 12 legend items reach `in-view` after scrolling into view, 0 console errors. | `groups-simple-scroll/index.html` |
| 6e. Revision: thriller tile uses group color | Designer | Done | The lone high-scoring draw (Algeria 3–3 Austria, Group J) in the Zero Club grid was hardcoded yellow. Replaced with a `--tile-accent` CSS custom property set per-tile from `GROUP_COLORS[m.group]`, so the border, glow, and score text match Group J's palette color instead of a fixed yellow. Verified computed styles resolve to `rgb(173,105,196)` = `#ad69c4` (Group J), matching the legend and dots exactly; 0 console errors. | `groups-simple-scroll/index.html` |
| 6f. Revision: pitch background + hero field markings | Designer | Done | Replaced the flat navy `.floodlit` body background with a mowed-stripe green gradient (repeating-linear-gradient stripe texture over a dark-green vertical gradient, floodlight radial glows layered on top) applied page-wide, contrast-checked against surface tones (text-dim stayed ≥4.1:1 even at the lightest stripe tone). Added an inline SVG (`.pitch-lines`, viewBox 1050×680 scaled from real 105m×68m pitch proportions) behind the hero title: touchline boundary, halfway line, center circle + spot, both penalty boxes, six-yard boxes, penalty spots, penalty arcs ("D"s) computed from real circle-line intersection geometry, four corner arcs, and goal-post rectangles at each touchline. Verified rendering via headless Chromium screenshot: pitch lines render correctly (arcs bulge outward as expected), text stays legible over the low-opacity (0.16) line strokes, and card panels keep their navy contrast against the green page throughout every section. | `groups-simple-scroll/index.html` |
| 6g. Revision: typography | Designer | Done | User feedback: heading font looked generic (system condensed stack). Introduced two Google Fonts loaded via `<link>` (Barlow Condensed 700–900 + italics for display; Inter 400–700 for body), set as `--font-display`/`--font-body` CSS variables. Applied the display font (weight 900, italic) to h1–h3, `.kicker`, `.section-kicker`, and every "scoreboard" number (hero stat figures, callout scores, draw-tile scores, tooltip score) for a bold slanted broadcast-graphic look; every other readable text element (subtitle, section copy, captions, legends, tooltips, caveats, footer) now explicitly uses the Inter body font instead of generic `system-ui`. Verified rendering via headless Chromium screenshot across all sections — fonts load and apply correctly, body copy stays clean and legible. | `groups-simple-scroll/index.html` |
